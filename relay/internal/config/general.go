package config

// GeneralConfig is kept for future configuration expansion
// Currently empty as listening port and environment were moved to appropriate locations
type GeneralConfig struct {
	// Reserved for future general configuration options
}
